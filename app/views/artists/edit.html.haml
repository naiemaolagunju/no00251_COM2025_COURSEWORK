%h1= I18n.t('artist.edit.name')
%p= I18n.t('artist.edit.subtitle')

= render 'form'

= link_to 'Show', @artist, :class => "btn btn-primary full-width"
\|
= link_to 'Back', artists_path, :class => "btn btn-primary full-width"
