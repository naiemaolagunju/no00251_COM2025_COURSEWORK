%h1= I18n.t('songs.edit.name')
%p= I18n.t('songs.edit.subtitle')

= render 'form'

= link_to 'Show', @song, :class => "btn btn-primary full-width"
\|
= link_to 'Back', songs_path, :class => "btn btn-primary full-width"
